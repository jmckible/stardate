%h1 Edit Recurring Transaction

= form_for @recurring do |f|
  = render partial: 'form', object: f
  %p
    = submit_tag 'Update'
    or
    %span.delete= link_to 'delete', @recurring, :method=>:delete