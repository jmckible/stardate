%h1 Vendor

%table
  %tr
    %th Name
    %th Count
    %th Amount

  - @vendors.each do |vendor|
    %tr
      %td= link_to vendor.name, vendor
      %td.num= number_with_delimiter Current.household.transactions.visible_by(Current.user).from_vendor(vendor).count
      %td.num= number_to_currency Current.household.transactions.visible_by(Current.user).from_vendor(vendor).sum(:amount), precision: 0

= will_paginate @vendors
